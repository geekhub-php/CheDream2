{% extends 'GeekhubResourceBundle::layout.html.twig' %}

{% block content %}

    <a href="{{ path('edit_dream', { 'slug' : form.vars.value.slug }) }}" class="btn btn-success">EDIT</a>

{% endblock %}
