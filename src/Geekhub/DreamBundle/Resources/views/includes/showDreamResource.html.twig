<div>
    <p class="bolder115">Кошторис проекту:</p>
    <hr class="show-hr">

    <div> {# financial #}

        <p class="text-center bolder115"><span class="glyphicon glyphicon-usd"> </span> Фінансові витрати</p>
        <ul class="list-group">
            {% for finance in dream.dreamFinancialResources %}
                <li class="list-group-item">
                    <span class="badge">{{ finance.quantity }} грн.</span>
                    {{ finance.title }}
                </li>
            {% endfor %}
        </ul>

    </div>

    <div> {# equipment #}

        <p class="text-center bolder115"><span class="glyphicon glyphicon-wrench"> </span> Інструменти/обладнання</p>
        <ul class="list-group">
            {% for equipment in dream.dreamEquipmentResources %}
                <li class="list-group-item">
                    <span class="badge">{{ equipment.quantity }} {{ equipment.quantityType }}</span>
                    {{ equipment.title }}
                </li>
            {% endfor %}
        </ul>

    </div>

    <div>  {# work #}

        <p class="text-center bolder115"><span class="glyphicon glyphicon-user"> </span> Роботи та ресурси</p>
        <ul class="list-group">
            {% for work in dream.dreamWorkResources %}
                <li class="list-group-item">
                    <span class="badge">{{ work.quantity }} чол. / {{ work.quantityDays }} дн.</span>
                    {{ work.title }}
                </li>
            {% endfor %}
        </ul>

    </div>

</div>