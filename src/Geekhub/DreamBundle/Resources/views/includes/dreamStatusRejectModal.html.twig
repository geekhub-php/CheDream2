<div class="modal fade" id="dreamStatusRejectModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                <h4 class="modal-title">Опис причини повернення мрії на доопрацювання.</h4>

            </div>
            <div class="modal-body">

                    {{ form_start(rejectForm, {'action': path('reject_dream', {'slug': slug}), 'method': 'POST', 'attr': {'role': 'form', 'novalidate': 'novalidate' } } ) }}

                    {{ form_row(rejectForm.rejectedDescription) }}
                    {{ tinymce_init() }}

                    <div class="modal-footer">
                        <p class="help-block">* Поле обов'язкове для заповнення</p>
                        <button type="button" class="btn btn-default" data-dismiss="modal">{{ 'dream.cancel'|trans }}</button>
                        <button type="submit" class="btn btn-danger">{{ 'dream.status.reject'|trans }}</button>
                    </div>

                    {{ form_end(rejectForm) }}
                <br/>
            </div>
        </div><!-- /.modal-content -->
    </div><!-- /.modal-dialog -->
</div><!-- /.modal -->