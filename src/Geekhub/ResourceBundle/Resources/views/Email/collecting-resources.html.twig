{% extends 'GeekhubResourceBundle:Email:layout.html.twig' %}

{% block header %}
    Вітаємо! Мрія, яку ви створили, була підтверджена адміністратором.
{% endblock header %}

{% block content %}
    <h3 class="centered">
        {% block description %}
            Мрія вже доступна на сайті і готова до збору коштів.
        {% endblock description %}
    </h3>

    <table class="layout centered">
        <tr>
            <td>
                <img src="{{ dream.mediaPoster|mediaPath|imagine_filter('dream_pictures_view_form', true) }}" />
            </td>
            <td>
                <h2><b>{{ dream.title }}</b></h2>
                <p><i>створена {{ dream.createdAt|date("m-d-Y") }}</i></p>
            </td>
        </tr>
    </table>
    <br/>

    <div class="centered">
        <h4>
            {% block alert_title %}
                Саме час поділитися новиною з друзями та розпочати збір ресурсів.
            {% endblock alert_title %}
        </h4>
        {% block alert %}
        {% endblock alert %}
        <table style="margin-left: auto; margin-right: auto">
            <tr>
                <td><button class="btn btn-default">Facebook</button></td>
                <td><button class="btn btn-default">VK</button></td>
                <td><button class="btn btn-default">Однокласники</button></td>
            </tr>
        </table>
    </div>

{% endblock content %}
